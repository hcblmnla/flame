## Flame

The Chaos game implementation

### Example

![fhd](generated/fhd.png)

### Usage

```shell
java -jar flame.jar -h
Usage: flame [-hV] [-f=<extension>] [-i=<iterations>] [-r=<resolution>]
             [-s=<samples>] [-sym=<symmetries>] [-t=<threads>]
             [-v=<variations>...]...
  -f, --format=<extension>
  -h, --help                 Show this help message and exit.
  -i, --iterations=<iterations>

  -r, --resolution=<resolution>

  -s, --samples=<samples>
      -sym, --symmetries=<symmetries>

  -t, --threads=<threads>
  -v, --variations=<variations>...

  -V, --version              Print version information and exit.
```
